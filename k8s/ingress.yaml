apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spike-api
  namespace: spike
spec:
  ingressClassName: nginx
  rules:
    - host: spike-api.<CLUSTER_IP>.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: spike-api
                port:
                  number: 8000
